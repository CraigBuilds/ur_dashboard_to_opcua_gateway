[pytest]
testpaths =
    tests/architecture
    tests/unit
    tests/system
addopts = -ra
cache_dir = .pytest_cache
pythonpath =
    .
    code/src
markers =
    system: container-backed system tests
